//CS2600 Number Guessing Game

#include <stdio.h>

int main() {

    // Display menu options 1-3 

    // Prompt user to enter a number for option 1-3

    // If user selects option 1
    // Randomly generate number
    // Prompt user to guess a number
    // if the user guesses correctly display win prompt and return to menu.
    // if incorrect tell the user if they are lower or higher than the number and prompt them to guess again.
    // if user enters 'q' return to menu

    // If user selects option 2
    // Display max value user can set number to.
    // Prompt user to enter a number greater than 0 and less than max

    // If user selects option 3
    // Display exit prompt and quit program

    return 0;
}